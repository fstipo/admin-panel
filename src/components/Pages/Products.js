import React from 'react';
import Container from '../Container';
import Sidebar from '../Sidebar';
import Header from '../UI/Header';

const Products = () => {
  return (
    <Container>
      <Header name="Products" icon="grid" />
    </Container>
  );
};

export default Products;
